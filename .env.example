# Copy to `.env` (which is gitignored) and fill in.

# Scraper writes — needs the SECRET key (sb_secret_...). Server-side only.
SUPABASE_URL=https://fjxbwerapxmgppqlpmhw.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...

# UI reads — uses the publishable key. Hardcoded in src/lib/supabase.js
# (no need to put it here; publishable keys are designed to be in browsers).

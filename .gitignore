# dependencies
node_modules/

# build output
dist/
dist-ssr/

# env / secrets
.env
.env.*
!.env.example

# editor / OS
.vscode/
.idea/
.DS_Store
*.log
npm-debug.log*

# Python (for scrapers/)
__pycache__/
*.pyc
.venv/
venv/

# Local scraper snapshot (used for debugging — Phase 2 will write to Supabase, not a file)
scrapers/_local_snapshot.json

# old reference project — NOT part of clt-plus repo
Aiforus_SaaS_pc/
